import 'package:flutter/material.dart';

class WeatherInformationViewLandscape extends StatelessWidget {
  const WeatherInformationViewLandscape({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
